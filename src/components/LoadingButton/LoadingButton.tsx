import { LoadingWrapper, Spinner } from './LoadingButton.styles'

const LoadingButton = () => {
  return (
    <LoadingWrapper>
      <Spinner />
    </LoadingWrapper>
  )
}

export default LoadingButton
